# 关于性能

优化最重要的事情之一就是优化内存访问；

## 内存

### 栈和堆

栈分配像是CPU指令，移动栈帧，得到内存地址，拷贝数据到内存中，栈比较快；
堆分配需要申请，调用malloc查询程序开始的freelist列表，看是否有符合条件的空闲内存，没有就要进一步像操作系统申请，需要维护一大堆结构；
通常来讲栈不会有cache miss，但是堆会有
本质的区别还是在分配上
**尽可能使用栈分配，除非条件不允许或者太大了**


## 基准测试

注意测试时编译器对代码的优化
确保分析的代码，是在发布时真正有意义的代码

可以使用RAII Timer 加



