# 数据结构

## 数组Array

同类型的数据集合
注意边界检查，C++在release模式下数组越界可以通过编译
数组在内存上是连续的
"xxx"双引号修饰的字符串，表示是一个const char array，不能被修改

## string

### 使用Char*
C++以Ascii方式存储字符串
0作为字符串的结束符

### 使用String

注意作为参数传递的过程，避免值传递造成的拷贝开销
```C++
void printString(const string& str) {
}

//注意字符串在内存中的存储，放在了只读的常量区
char *str = "hello world";
str[2] = 'a';   //error，无法修改常量区的内存

char str[] = "hello world";
str[2] = 'a';   //ok，实际上是把常量区的字符串拷贝到栈上


```

C++14 引入了string_literal  


### 不同类型的char字串

注意赋值方式的区别
>1. char
>2. wchar_t  //宽字符，大小由编译平台决定
>2. char16_t
>2. char32_t
```C++
wchar_t* wstr = L"hello world";
```

## Vector

优化：主要是自动扩容时，复制原内存到新内存，这个过程有性能损耗，尤其是不断有这种类型操作
如何避免拷贝操作
**避免拷贝过程中，在传递过程中产生的新的对象**
**避免vector重新扩容，扩容时会被所有对象重新拷贝一次**
```C++
struct Pos{
    int x, y;
    Pos(int x, int y):x(x), y(y){}
    Pos(const Pos& other):x(other.x), y(other.y)
    {
        cout<<"Copy"<<endl;
    }
}

vector<Pos> posList;
posList.reserve(3);                 //提前指定大小，避免vector自动扩容
postList.push_back(Pos(1, 2));      //传递过程中，会调用拷贝构造函数
postList.emplace_back(3, 4);        //使用提供的参数直接构造对象
```

## array

相对于vector，array是处理静态数组的，数组大小、类型在编译期确定
数组大小实际上是一个模板参数，size函数直接返回数组大小，并不是一个变量
相较于C语言形式的数组，差别在哪里：
>1. 有size便于遍历
>2. 有边界检查，不用手动维护


## 多维数组

类型只是一种语法，设置类型是用来处理数据的；
尽量避免使用多维数组，容易带来cache miss问题