# 进阶语法

[智能指针](#智能指针)
[多返回值](#处理多返回值)
[模板template](#模板template)
[宏](#宏)
[函数指针](#函数指针)
[lambda](#lambda)
[线程](#线程)
[计时](#计时)
[类型双关](#类型双关)
[联合体](#联合体)
[类型转换](#类型转换)
[结构化绑定](#结构化绑定)
[optional](#optional数据)
[单一变量存放多种类型数据](#单一变量存放多种类型数据)


## 智能指针

指针指针，可以自动管理内存，实际上只是raw pointer的包装


### unique_ptr

域内自动管理，离开作用域自动回收
不能copy，底层实际上是将拷贝构造函数、赋值函数设置为delete


### shared_ptr

具体实现会随着编译器变化，底层机制是引用计数


### weak_ptr

来自shared_ptr的拷贝和复制不会增加引用计数


## 处理多返回值

C++提供的处理方式：
1.参数里面加入引用，函数里面对引用赋值；
2.同类型的对象返回一个数组指针；
3.返回std::array<T,N>, std::vector<T>, array是在栈上构建的，理论上更快；
4.返回对象中包含不同类型，使用std::tuple

处理方式：
1.使用结构体，结构体可以扩展，变量名的存在也增加了可读性


## 模板template

模板比其它语言（泛型）要牛逼的多
模板有点像宏，黑科技多，但是泛型非常受制于类型系统
解释：模板允许定义一个根据用途进行编译的模板（让编译器基于某套规则生成代码），简单来讲创造一个blueprint
如果发生了实际调用才会生成对应实际参数的代码（这个地方check由编译器决定）
**编译时会根据实际调用生成对应的代码**
meta programming元编程，编译器在编译时实际在进行编程

### 关于使用
不要一棒子打死，实际上还是很好用的
在日志系统或某种其它的使用场景下，比较好用
模板搞得太复杂，不知道在干嘛

```C++
template<typename T>
void print(T value) {
    cout << value << endl;
}
print(1);
print<string>("hello");

//可以使用具体的类型，本质还是在编译期实际调用时的替换生成
template<typename T, int N>
class Array{
    T arr[N];
}
Array<5> a;
```

## 宏

宏对于调试很nice
使用预处理来**宏化**某些操作，避免一遍又一遍来重复输入
本质上是文本的查找和替换
**正确的编程思路：不炫技，不要写令人迷惑的代码，努力写出好代码**
```C++
//简单的日志系统
#if _DEBUG = 1
#define Log(x) cout << "debug:"<< x << endl
#elif defined(_RELEASE)
#define Log(x)
#endif

Log("hello");
```

## 函数指针

一开始的理解：函数就是一个黑盒，输出=>输出
函数实际上可以赋值给变量，可以作为参数传递给其它函数

```C++
void hello() {}
typedef void(*Func)();

auto f = hello; //使用auto自动推导
f();
Func f2 = hello;    //使用typedef
f2();

//标准的使用方法，告诉函数在特定场合干嘛
void foreach(int arr[], void(*func)(int))
{
    for (int i = 0; i < 5; i++)[
        func(arr[i]);
    ]
}

```

## lambda

匿名函数，视为变量快速的一次性函数
语法：[capture](params)->return_type{body}
作为参数传递时，不能使用原生的函数指针，需要使用std::function封装
注意捕获的多种形式


## 线程

```C++
#include <thread>

void doWork(){}

int main()
{
    std::thread t(doWork);
    t.join();   //阻塞当前线程，直到t执行完
}
```

## 计时

chrono库 一个高效的C++ 时间库
一个timer例子
```C++
#include <chrono>
//使用C++对象生存周期和chrono库来做一个定时器
struct Timer
{
    std::chrono::time_point<std::chrono::high_resolution_clock> start, end;
    std::chrono::duration<float> duration;
    
    Timer(){
        start = std::chrono::high_resolution_clock::now();
    }

    ~Timer(){
        end = std::chrono::high_resolution_clock::now();
        duration = end - start;
        std<<cout <<"Timer: " <<duration.count() <<'\n';
    }
}

Function(){
    Timer timer;
    // do something
}

```

## 类型双关

将拥有的内存，当作不同类型的内存来对待
绕过C++类型系统，虽然类型是由编译器强制执行的，但是可以直接访问内存
```C++
//double 8bytes，int 4bytes
//大小不同，这样的转换及之后的赋值不安全
int a = 5;
double b = *(double*)&a;


//更实际的例子
struct Entity{
    int x, y;
    int* getPos(){
        //常规做法
        int *array = new int[2];
        array[0] = x;
        array[1] = y;
        return array;

        //性能优化
        return &x;
    }
}

Entity e = {5, 10};
//实际上Entity在内存之中的布局可视作一个int数组
int* pos = (int*)&e;
pos[0] = 10;   //e.x
int y = *(int *)((char*)&e + 4);    //int y = e.y;   
// 了解了内存布局和C++对于内存的访问规则，都是一样的事情
// 按照我想的方式，将内存中的数据取出来，按照想要的方式进行组织

```


## 联合体

一个联合体只能有一个成员，不同变量共用同一块内存
做的事情，通常与类型双关
匿名union不能有成员函数
```C++
struct Vector2{
    int x, y;
}

struct Vector4{
    union{
        struct{
            int x, y, z, w;
        };
        struct{
            Vector2 a, b;
        }
    }
}

Vector4 v = {1, 2, 3, 4};
v.x = 5;
v.a.x = 5;
```


## 类型转换

C++强类型语言，不能改变类型的使用，除非存在某种转换
隐式转换、显示转换

C++转换，实际上是加了点语法糖，有些情况做了点额外事情
static_cast：编译时检查，做类型转换
reinterpret_cast：把内存重新解释成别的东西（类似于类型双关）
const_cast：移除或添加变量的const限定
dynamic_cast：运行时检查，类型转换是否成功
建议使用C++风格，使代码更可靠，对他人友好

```C++
//C语言风格
double value = 5.25;
int a = (int)value;

//C++风格
int b = static_cast<int>(value);

```

### dynamic_cast

作为一种安全机制提供，做特定类型的转换，内部做了一些验证以保证转换是有效的
可以用作检查类型
dynamic_cast需要有虚函数表支持
能进行类型区分，实际上是因为存储了运行时类型信息(RTTI)
```C++
class Entity{
public:
    virtual void Print(){}
}
class Player : public Entity{}
class Enemy : public Entity{}

//隐式转换很容易
Entity* e = new Player();
Entity* e1 = new Enemy();
//向下转换很难
Player* p = e;  //error， e可能是个enemy
//需要向编译器保证e是一个player
Player* p1 = (Player*)e1;  //实际上这种操作很危险，因为e可能是enemy

Player* p2 = dynamic_cast<Player*>(e1);
if(dynamic_cast<Player*>(e1)){
    //类似于java中的 A instanceof Class
}
```

## 结构化绑定

C++17新特性，更好处理多返回值的方式

用struct:可能只有一个地方用到了定义，处理某个返回值的时候（这种实在是没必要）
如无必要，勿增实体

```C++
tuple<string, int> createPerson(){ 
    return {"hello", 5};
}

//旧方法
tuple<string, int> person = createPerson();
auto name = get<0>(person);
auto age = get<1>(person);

string name;
int age;
tie(name, age) = createPerson();

//使用struct

//C++ 17 结构化绑定
auto [name, age] = createPerson();

```

## optional数据

处理一个非期待的返回值，处理可能存在、可能不存在的情况；
```C++
//普通版本
std::string readFile(const std::string& path, bool& isSucceed)
{
    std::ifstream file(path);
    if(file){
        std::string res;
        //read file
        file.close();
        isSucceed = true;
        return res;
    }
    isSucceed = false;
    return std::string();
}

bool isOpenSucceed;
auto data = readFile("file.txt", isOpenSucceed);
if(isOpenSucceed){ 
    //do something
}


//optional版本
std::optional<std::string>readFile(const std::string& path)
{
    std::ifstream file(path);
    if(file){
        std::string res;
        //read file
        file.close();
        return res;
    }
    return {};
}


auto data = readFile("file.txt");
std::string value = data.value_or("not_present");
if(data.has_value()){       //这里的处理比较好
    //do something
}

```


## 单一变量存放多种类型数据

variant，不用担心处理的确切数据类型
实际上是结构体，将对应多个类型存储为结构体中的成员
```C++
//对应的类型会映射成为索引
std::variant<std::string, int> data;
data = "hello";
data.index(); //0
data = 5;
data.index(); //1
//使用data.index可以判断当前类型

//使用get_if
if(auto p = std::get_if<std::string>(&data)){
    std::string& value = *p;
}
```


